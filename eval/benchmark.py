"""benchmark — Bridge throughput and latency benchmark (M1 measured exit number).

Measures: transitions/s/arena, p99 Node→Python round-trip at the 200 ms
decision interval, damage-event boundary correctness (no drop/double-count),
and the maximum number of arenas the dev laptop can sustain at ≥19 TPS over
a ≥10-minute run.  Reports results as structured JSON so they can be committed
as the AC4 evidence artifact.

Owner: T11 (Eval/infra track)
# TODO(T11): implemented by task T11
"""

# TODO(T11): implemented by task T11
