# opponents/

Scripted opponent implementations. Contains the abstract base class for all
opponents (`base.py`) and the stationary idle dummy used for M1/M2 evaluation
(`dummy.py`). Deferred opponents (scripted bot, snapshot pool for self-play) are
stubbed here but out of scope for kickoff.

**Owner workstream:** Reward/opponent track
