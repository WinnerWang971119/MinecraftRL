"""snapshot_pool — Prioritized past-self snapshot pool for self-play (M4 target).

DEFERRED — out of scope for kickoff. This is a skeleton stub.

Will maintain a pool of frozen network snapshots sampled by PFSP (Prioritized
Fictitious Self-Play) as opponents.  Targets the M4 milestone; implementation
deferred until after M3 and the self-play design is finalized.

Owner: M4 (Reward/opponent track)
# DEFERRED(M4): out of scope for kickoff
"""

# DEFERRED(M4): out of scope for kickoff
