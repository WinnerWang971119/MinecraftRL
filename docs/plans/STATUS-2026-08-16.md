# Demo-day push — state as of 2026-08-16

Branch `feat/demo-turn-actions-scripted-opponent`, 42 commits ahead of `origin/main`.
Plan: `docs/plans/2026-08-16-demo-scripted-opponent-exhibition.md`. Deadline **2026-08-20**.

## Committed (13 of the 16 code tasks)

| Task | Commit | What it does |
|---|---|---|
| T4 Java pin | — | already satisfied by PR #32; no change needed |
| T9 ScriptedBot | `e94c61e` | heuristic opponent, EASY/HARD presets, seeded RNG |
| T1 opponent seam | `c2ba036` | `_opponentHandle()` so ATTACK/turn work against a human |
| T10 bot tests | `ba8f2c5` | 53 cases over the spec-7.2 ladder |
| T2 death detection | `180ad41` | `rl_deaths` scoreboard read off raw client packets |
| T3 exhibition mode | `dd08b7f`, `c144bf1` | no timeout, no auto-restart, first-claimant latch |
| T11a wire field | `9e15aca` | `opp_action` + swing report across all three contract forms |
| T11b opponent actions | `3aa6b7e` | second `MacroExecutor` in the same decision window |
| T5 launcher | `bd811ba` | one command: Paper + bridge + greedy agent |
| T12 curriculum | `78be355` | per-arena scripted bots, gated EASY/HARD mixture |
| T6 reset command | `0cb9af6` | request file, re-drives play in-process, heals the human |
| T11c knockback/speed | `29fcf10` | bridge overrides both attributes, with read-back |
| T14 truthful comments | `b8615f8` | guard test + five corrected false comments |
| T7 reflex shield | `30fe90d` | exhibition-only re-acquire after N blind steps |
| T17 pad sweep | `a42e6cc` | **measured: 25 pads, 121.95 transitions/s** |

Suites: **Python 1023 passed / 2 deselected**, **bridge 246 passed**.

## In flight when this was written

- **T13** — warm-start + checkpoint selection. Carries four blockers (see plan's T13 row).
- **T7 review**, **final stale-comment sweep**. Both cosmetic-risk.

Working tree is dirty with T13's uncommitted work (`agent/train.py`, `agent/train_config.py`,
`eval/evaluate.py`, `tests/test_warm_start_selection.py`) plus the comment sweep
(`agent/actions.py`, `bridge/actions.js`, `bridge/bot.js`, `bridge/bot.test.js`).

## Not started

- **T16** — exploration constants. `eps_decay_episodes=200` is single-arena sizing; all arenas
  share the global counter, so at 25 pads ε floors after ~8 episodes/arena (~1% of a day's run).
  **Must land before any overnight training.**
- **T15** — README / RUNBOOK / demo-day guide, including the assisted-turn disclosure (AC14).
- **T8** — live human rehearsal. **Needs a second person**; nothing substitutes for it.

## Before launching a training run

1. **T13's blockers must be closed.** With defaults the run stops after ~1000 grad steps on a
   fake pass (the eval scores against a stationary opponent); with `--eval-every-grad-steps 0`
   it saves zero checkpoints. Neither workaround was CLI-reachable.
2. **T16's ε sizing.**
3. **Measure the gradient-step rate** in a smoke run and set `eval_every_grad_steps` from it —
   the default 1000 is unmeasured in wall-clock, and it currently also governs checkpoint cadence.
4. **AC18's last inch**: `agent/train.py:2888` builds `SubprocessArenaLauncher` without
   `dummy_knockback_immune=False`, so a scripted run still gets an immune, speed-0 opponent.

## The load-bearing decision

`N_ACTIONS` stays **8**. That is why `runs/m2_multi.pt` still loads and the demo exists
independently of whether any retrain finishes. Do not expand the action space before the 20th.
