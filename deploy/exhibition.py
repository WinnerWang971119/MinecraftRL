"""exhibition — Exhibition/booth deployment launcher (DEFERRED — skeleton stub).

DEFERRED — out of scope for kickoff. This is a skeleton stub.

Will launch the trained agent in a human-vs-agent exhibition match for the
human-subject study.  Fills after M2 is achieved and the study protocol and
IRB approval are in place.

# DEFERRED(deploy): out of scope for kickoff
"""

# DEFERRED(deploy): out of scope for kickoff
