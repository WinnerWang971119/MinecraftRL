"""actions — Action macro enum (frozen day-1 contract artifact).

Defines the discrete action space as an enum of named macros (e.g., MOVE_FORWARD,
STRAFE_LEFT, ATTACK, JUMP).  Every component that issues or interprets actions
(env, agent, bridge) must import from here.  The enum is frozen once T4 merges;
changes require a PR visible to all tracks.

Owner: T4 (DQN core track)
# TODO(T4): implemented by task T4
"""

# TODO(T4): implemented by task T4
