"""reward_config — Reward shaping weights and hyperparameters.

Centralizes all reward component weights (hit-landing bonus, aim bonus,
survival bonus, death penalty, timeout penalty, etc.) so tuning a reward
signal requires changing only this file.  Imported by both reward.py and
the training config.

Owner: T5 (Reward/opponent track)
# TODO(T5): implemented by task T5
"""

# TODO(T5): implemented by task T5
