"""train_config — Training hyperparameters dataclass.

Centralizes all training knobs: learning rate, batch size, sequence length,
burn-in length, n-step return horizon, PER alpha/beta, ε decay schedule,
target-net update frequency, and checkpoint interval.  Populated by T16;
separate from contract_config.py (which holds frozen interface constants).

Owner: T16 (DQN core track)
# TODO(T16): implemented by task T16
"""

# TODO(T16): implemented by task T16
