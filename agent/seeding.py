"""seeding — Determinism helpers: seed propagation across Python, NumPy, and PyTorch.

Provides a single seed_everything(seed: int) function that sets seeds for the
Python random module, NumPy, and PyTorch (CPU + CUDA) in one call.  Also seeds
the Gym env if provided.  Required by T6 to make reproducible runs possible.

Owner: T6 (DQN core track / shared contract)
# TODO(T6): implemented by task T6
"""

# TODO(T6): implemented by task T6
