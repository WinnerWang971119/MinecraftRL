# tests/

Pytest unit and integration test suite. Unit tests run against hand-authored
fixtures with no live server; integration tests run against a live Paper server
and bridge. The perception-filter leak battery (AC5) and the LSTM unit fixture
(TC8b) both live here.

**Owner workstream:** all tracks
