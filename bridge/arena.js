// arena.js — Arena management: multi-arena lifecycle (DEFERRED).
//
// DEFERRED — out of scope for kickoff. This is a skeleton stub.
//
// Will manage N parallel arena instances: spawning per-arena bot pairs,
// routing messages between arenas and the Python side, and load-balancing
// resets.  Single-arena operation is handled directly in bot.js for kickoff.
//
// TODO(DEFERRED): out of scope for kickoff; multi-arena targets post-M2 scaling
