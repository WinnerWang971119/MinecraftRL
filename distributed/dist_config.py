"""dist_config — Distributed system configuration (DEFERRED — skeleton stub).

DEFERRED — out of scope for kickoff. This is a skeleton stub.

Will hold configuration for the distributed actor/learner setup: number of
actors, arena addresses, transport backend selection, weight-sync frequency,
and replay buffer sharding.  Targets the post-M2 distributed scaling milestone.

# DEFERRED(distributed): out of scope for kickoff
"""

# DEFERRED(distributed): out of scope for kickoff
