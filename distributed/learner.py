"""learner — Distributed learner worker (DEFERRED — skeleton stub).

DEFERRED — out of scope for kickoff. This is a skeleton stub.

Will implement a dedicated learner process that receives experience batches
from N actor workers via ExperienceTransport, updates the network, and
broadcasts updated weights back to actors.  Targets the post-M2 distributed
scaling milestone.

# DEFERRED(distributed): out of scope for kickoff
"""

# DEFERRED(distributed): out of scope for kickoff
