# distributed/

DEFERRED — out of scope for kickoff. This directory is a skeleton only.

Placeholder for the distributed actor/learner architecture: the
`ExperienceTransport` interface seam, actor workers, learner workers, and
serialization utilities. Only `LocalTransport` (single-process) will be built
during the kickoff phase; the full distributed stack targets post-M2 scaling.

**Owner workstream:** DEFERRED — out of scope for kickoff; skeleton only
