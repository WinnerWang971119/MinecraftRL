"""actor — Distributed actor worker (DEFERRED — skeleton stub).

DEFERRED — out of scope for kickoff. This is a skeleton stub.

Will implement an actor process that runs the policy, collects experience from
a remote arena, and pushes batches through ExperienceTransport to the learner.
Targets the post-M2 distributed scaling milestone.

# DEFERRED(distributed): out of scope for kickoff
"""

# DEFERRED(distributed): out of scope for kickoff
