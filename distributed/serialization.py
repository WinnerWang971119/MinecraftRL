"""serialization — Experience batch serialization for network transport (DEFERRED).

DEFERRED — out of scope for kickoff. This is a skeleton stub.

Will implement fast serialization/deserialization of numpy experience batches
(e.g., using msgpack or shared memory) for the distributed transport layer.
Targets the post-M2 distributed scaling milestone.

# DEFERRED(distributed): out of scope for kickoff
"""

# DEFERRED(distributed): out of scope for kickoff
