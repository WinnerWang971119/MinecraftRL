# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/.venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ---- PyTorch / ML artifacts ----
*.pt
*.pth
*.ckpt
checkpoints/
wandb/
runs/
tensorboard/
*.tfevents.*

# ---- Node ----
node_modules/
npm-debug.log*
yarn-error.log*

# ---- Java / Paper / Minecraft server runtime ----
server/*.jar
server/world/
server/world_nether/
server/world_the_end/
server/logs/
server/crash-reports/
server/cache/
server/libraries/
server/versions/
server/plugins/
server/banned-*.json
server/usercache.json
server/eula.txt
server/.console_history

# Paper-generated / setup-script-generated config (server.properties is written
# by server/setup/setup.ps1; the rest Paper regenerates on first boot). The
# source of truth is the setup script + the committed arena datapack, not these.
server/server.properties
server/bukkit.yml
server/spigot.yml
server/commands.yml
server/help.yml
server/permissions.yml
server/config/
server/version_history.json
server/whitelist.json

# Multi-arena runtime: start-arenas.ps1 materializes server/arenas/arena-<i>,
# each a full Paper runtime. All generated; source of truth is the setup script
# + the committed arena datapack.
server/arenas/

# ---- Logs / scratch ----
*.log
scratch/
tmp/

# ---- OS / editor ----
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~
